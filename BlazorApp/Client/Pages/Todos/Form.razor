<EditForm Model="@todo" OnValidSubmit="@OnValidSubmit">
    <DataAnnotationsValidator />
    <div class="form-group">
        <label>Date :</label>
        <div>
            <InputDate @bind-Value="@todo.Date" />
            <ValidationMessage For="@(() => todo.Date)" />
        </div>
    </div>
    <div class="form-group ">
        <div>
            <label>Title :</label>
            <div>
                <InputText @bind-Value="@todo.Title" style="width:80%"></InputText>
                <ValidationMessage For="@(() => todo.Title)" />
            </div>
        </div>
    </div>
    <div class="form-group ">
        <div>
            <label>Description :</label>
            <div>
                <InputTextArea @bind-Value="@todo.Description" style="width:80%; height:100px"></InputTextArea>
                <ValidationMessage For="@(()=> todo.Description)"></ValidationMessage>
            </div>
        </div>
    </div>
    <button type="submit" class="btn btn-success"> @ButtonText </button>
</EditForm>
